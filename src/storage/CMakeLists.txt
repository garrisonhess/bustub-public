add_subdirectory(disk)
add_subdirectory(index)
add_subdirectory(page)
add_subdirectory(table)

# add_library(bustub_storage)

# set(ALL_OBJECT_FILES
#     ${ALL_OBJECT_FILES} $<TARGET_OBJECTS:bustub_storage>
#     PARENT_SCOPE)
